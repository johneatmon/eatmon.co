NEXT_PUBLIC_SITE_URL=http://localhost:3000

SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
SUPABASE_JWKS_URL=

# GitHub PAT with `read:packages` for private `@johneatmon/*` font packages.
# After setting this, run once: pnpm config set "//npm.pkg.github.com/:_authToken" "$GITHUB_PAT"
# (or: node scripts/setup-github-packages-auth.ts)
# Vercel: add GITHUB_PAT — installCommand already configures pnpm auth.
GITHUB_PAT=
